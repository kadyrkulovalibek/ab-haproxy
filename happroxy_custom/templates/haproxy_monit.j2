# /etc/monit/conf.d/haproxy

check process haproxy with pidfile /var/run/haproxy.pid
  start program = "/etc/init.d/haproxy start"
  stop program = "/etc/init.d/haproxy stop"
  if failed host 127.0.0.1 port 80
    then restart